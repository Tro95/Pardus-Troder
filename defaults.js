/**
 *	This file sets the default values for all settings used in the script.
 *
 *  If setting values on the options page doesn't work for some reason (e.g.
 *  mobile) then try adjusting values here instead.
 */

 var defaults = {
 	'unload_ship': true,
 	'preview_trade': true,
 	'use_magscoop': false,
 };